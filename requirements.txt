# Interface: apenas os módulos essenciais do Qt. O pacote completo "PySide6"
# traz também os Addons (motor de navegador, 3D, gráficos Qt...), que este
# aplicativo não usa e que somam mais de 400 MB de download e disco.
PySide6-Essentials>=6.6
anthropic>=0.40
openai>=2.0
claude-agent-sdk>=0.2
jupyter_client>=8
ipykernel>=6
nbformat>=5
# Versão FIXA, não piso. A pysus lançou cinco versões em três dias (2.10.0 a
# 2.10.4, agosto de 2026), e uma mudança no catálogo entre elas fez três dos
# nossos exemplos passarem a devolver o Brasil inteiro rotulado como Paraná —
# sem erro nenhum, só o número errado. Com piso aberto, duas instalações feitas
# em semanas diferentes se comportam de formas diferentes e o usuário não tem
# como saber. O número aqui é a versão contra a qual os exemplos do repositório
# foram validados; para subir, rode antes:
#     python _ferramentas/vigiar_pysus.py --nova
#
# 2.10.6: auditada em 29/08/2026 diferenciando os ARQUIVOS contra a 2.10.4,
# nao o CHANGELOG — que subestima o que mudou: a 2.10.6 aparece la so como
# "fix readthedocs build", mas o leitor de DBF passou a ler o arquivo em
# pedacos em vez de carrega-lo inteiro na memoria. E ganho real para nos, que
# perdemos o kernel por falta de memoria com frequencia.
# O que a auditoria confirmou: api/ftp/databases.py, api/_impl/databases.py,
# api/columns.py e api/types.py estao byte a byte identicos a 2.10.4 — os
# grupos do SIA, o filtro .csv da origem Saude e os metadados de coluna nao
# mudaram, entao nossas licoes seguem validas. A 2.10.5 removeu a base
# VIGILANCIAMEIOAMBIENTE, que nenhum exemplo nosso usa.
#
# Herdado da 2.10.4 e ainda valido: ela reconhece prefixos que a 2.10.3
# deixava sem grupo (ABO, ACF, ATD, SAD, AMP). Atencao: os nomes que ela da
# aos grupos NAO sao a nomenclatura do DATASUS — "AB" nao e Atencao Basica, e
# acompanhamento de cirurgia bariatrica. Ver as licoes 100 a 103.
pysus==2.10.6
# A pysus 2.10 importa "yaml" logo no carregamento do pacote, mas esqueceu de
# declarar o PyYAML entre as dependências dela. Sem esta linha, a instalação
# termina "com sucesso" e o aplicativo quebra no primeiro import com
# "ModuleNotFoundError: No module named 'yaml'". Quando a pysus corrigir isso,
# esta linha vira redundante — e inofensiva.
pyyaml>=6.0
# Obrigatório para a PySUS funcionar dentro de um notebook: as funções dela
# chamam asyncio.run(), que falha quando já existe um laço de eventos rodando.
nest_asyncio>=1.6
matplotlib>=3.8
openpyxl>=3.1
